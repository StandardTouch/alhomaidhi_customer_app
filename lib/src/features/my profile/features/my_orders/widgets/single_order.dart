import 'package:flutter/material.dart';

class SingleOrderCard extends StatelessWidget {
  const SingleOrderCard({super.key});
// @maaz - Add your code here....
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Single Order Card"),
    );
  }
}
